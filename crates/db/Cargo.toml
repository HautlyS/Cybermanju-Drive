[package]
name = "cybermanju-db"
version = "0.1.0"
edition = "2021"
description = "redb database layer with parent index"

[dependencies]
anyhow = "1"
cybermanju-types = { path = "../types" }
redb = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
